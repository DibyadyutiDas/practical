SOURCE CODE:

#include<stdio.h>

#include<conio.h>

main()

{ int bt[20], wt[20], tat[20], i, n; float wtavg, tatavg;

clrscr();

printf("\nEnter the number of processes scanf("%d", &n);

for(i=0;i<n;i++)

printf("\nEnter Burst Time for Process %d", i); scanf("%d", &bt[i]);

wt[0] wtavg <-0: tat[0] tatavg-bt[0]; for(i=1;i<n;i++)

wt[i]wt[i-1]+bt[i-1]; tat[i] wtavg tat[i-1]+b[i]: wtavg wt[i]: tatavg tatavgtat[i]:

printf("\t PROCESS BURST TIME WAITING TIME TURNAROUND TIME\n"); for(i=0;i<n;i++)

printffinit Pd it did it %d", i, bt[i], wt[i], tat[i]); printinAverage Waiting Time %f", wtavg/n): printf("\nAverage Turnaround Time %f", tatavg/n).

getch():